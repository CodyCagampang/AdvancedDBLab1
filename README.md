1. When using CouchDB, The ID/Rev ID Changes each time you make a change
2. When using the cURL command, don't forget to change the _id and _revid to the CouchDB JSON File
3. When in Postman, Go in AUTHORIZATION, Change from No Security --> Basic, And input admin and mtu12345 into the boxes on the right
4. When using Postman, When doing POST/DELETE/PUT don't forget to include the Database name with the ID
